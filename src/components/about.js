import React from "react";
import { checkPropTypes } from "prop-types";

const About = (props) => {
  return (
    <div id = "introduction">
      <p>About</p>
    </div>
  );
};

export default About;
